package com.valuablecode;

import static org.junit.Assert.assertEquals;

import org.junit.Test;


/**
 * Performs a simple validation to confirm that JUnit testing in Eclipse is configured as expected.
 */
public class ValidationTest {

	@Test public void
	validates_that_JUnit_is_configured_as_expected() {
	    assertEquals("Validation test", 0, 0);
	}

}
